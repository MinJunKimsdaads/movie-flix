import { useEffect } from "react";
import { firstFetchList, fetchList } from "../store/Store";
function List({menu}){
    // useEffect(()=>{
    //     firstFetchList(menu);
    // },[]);

    // useEffect(()=>{
    //     fetchList(menu);
    // },[menu]);

    return (
        <div>

        </div>
    )
}

export default List;