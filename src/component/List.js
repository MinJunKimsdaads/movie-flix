import { useEffect } from "react";
function List({menu}){
    
    const fetchList = () => {
        try{
            // const query = menu === 'all' ? '':`&category=${category}`;
        }catch(e){
            console.log(e);
        }
    }
    return (
        <div>

        </div>
    )
}

export default List;